# GitHub Personal Access Token
# Required scopes: repo (or public_repo for public repos only)
# Get your token at: https://github.com/settings/tokens
GITHUB_TOKEN=your_github_personal_access_token_here

# Database URL
# ローカル開発（SQLite）の場合:
# DATABASE_URL=file:./data/l10n-tracker.db
#
# Vercel Postgres / PostgreSQL の場合:
# DATABASE_URL=postgres://username:password@hostname:5432/database?sslmode=require
# Vercelでは自動的に設定されます
DATABASE_URL=postgres://username:password@hostname:5432/database?sslmode=require

# Environment
NODE_ENV=development

# Server Port (optional, ローカル開発時のみ)
PORT=3000

# Next.js Public API URL (optional)
# NEXT_PUBLIC_API_URL=https://your-app.vercel.app/api
